[pytest]
markers =
    api: Tests de API del backend (no requieren frontend)
    integration: Tests de integración API + lógica de negocio
    e2e: Tests end-to-end con Playwright (requieren frontend y backend)

addopts = -v --tb=short
testpaths = api integration e2e
